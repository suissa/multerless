## Absorbed Decisions

This folder contains decisions where external ideas were
reimplemented internally without merging external code.

Absorption preserves:

- canonical API
- security invariants
- dependency isolation
